
  function findPairWithSum(arr, targetSum) {
       
    return null;
  }
  const numbersArray = [1, 2, 3, 4, 5];
  const targetSum = 9;
  const result = findPairWithSum(numbersArray, targetSum);
  console.log(`Pair with sum ${targetSum}:`, result);

  